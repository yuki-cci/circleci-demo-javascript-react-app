version: 2.1
      
jobs:
  test:
    docker:
      - image: cimg/node:14.13.0
    steps:
      - checkout
      - run: 
          echo "hello world"
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run Tests
          command: npm run test

workflows:
  test-deploy:
    jobs:
      - test
